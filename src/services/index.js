import http from './http';
import {
  getPeople, getPerson, deletePerson, sendPerson, updatedPerson,
} from './people';

export default {
  http,
  getPeople,
  getPerson,
  deletePerson,
  sendPerson,
  updatedPerson,
};
